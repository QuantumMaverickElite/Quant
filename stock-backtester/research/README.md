# Research

`research/` contains experiments, comparisons, ablations, historical
reproducibility runners, and research-specific analysis.

- Reusable implementation belongs in `src/`.
- Stable user-facing commands belong in `scripts/`.
- Validation belongs in `tests/`.
- Repository maintenance belongs in `tools/`.

Current research family: [`combined_signals/`](combined_signals/).
